"""
app 包 — 通话语音情绪识别服务

本包实现了一个基于 HuBERT 预训练模型的通话语音情绪识别系统，
核心流程为：音频解码 → 分段切分 → 模型推理 → 六类→四类概率投影 → 加权聚合 → 结果输出。

模块划分：
- config   : 全局配置，基于 Pydantic Settings，支持 .env 文件和环境变量
- model    : 模型加载与推理运行时，线程安全的延迟加载，CUDA OOM 自动降级到 CPU
- audio    : 音频解码、重采样与分段切分，基于 FFmpeg subprocess
- analyzer : 情绪分析协调器，负责分段推理、概率投影与加权聚合
- schemas  : API 数据契约模型（Pydantic BaseModel），用于请求/响应的类型与校验
- errors   : 应用统一异常类，确保错误信息可安全返回给调用方
- main     : FastAPI 应用工厂与路由，支持 NDJSON 流式输出

设计原则：
1. 线程安全：模型加载使用双重检查锁定，分析请求使用互斥锁防止并发推理
2. 延迟加载：服务启动时不立即下载约 1.1 GB 模型权重，首次推理时才加载
3. 安全错误处理：AppError 仅携带可公开的 code/message/status_code，不泄露内部路径或堆栈
4. 流式输出：分析过程逐段推送进度事件，最后推送完整结果，便于前端实时展示
"""
